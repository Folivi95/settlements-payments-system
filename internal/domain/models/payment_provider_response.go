package models

type PaymentProviderResponse struct { // lint doesn't like unused thing, but i think we have a branch that _does_ use it
	// paymentTrackingId string
	// paymentStatus     PaymentInstructionStatus
}
